<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.willdev.funfacts.ComposeFactActivity">

   <EditText
       android:id="@+id/compose_fact"
       android:layout_width="match_parent"
       android:layout_height="wrap_content"
       android:background="#FFFFFF"
       android:hint="Compose New Fact!"/>

    <Button
        android:id="@+id/submit_fact_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/compose_fact"
        android:layout_marginTop="8dp"
        android:text="@string/submit_fact"/>

</RelativeLayout>
